<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinosoft.bank.broker.mapper.BkSaleAgentInputMapper">

    <select id="selectBkPageShowList"
            resultType="com.sinosoft.bank.broker.domain.vo.BkSaleAgentVo">
        select salecode,
               oldsalecode,
               managecom,
               (select shortname from ldcom lc where comcode = a.managecom) managecomname,
               agentcom,
               (select name from lacom lc where lc.agentcom = a.agentcom)   agentcomname,
               name,
               employdate,
               outworkdate,
               agentstate,
               mobile
        from lasaleagent a
            ${ew.getCustomSqlSegment}
    </select>
</mapper>
